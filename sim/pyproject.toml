[project]
name = "sim"
version = "0.1.0"
description = "Crazyflow simulation environment"
requires-python = ">=3.13"
dependencies = [
    "crazyflow",
    "numpy",
    "pynput>=1.8.1",
]

[tool.uv.sources]
crazyflow = { git = "https://github.com/utiasDSL/crazyflow.git" }
