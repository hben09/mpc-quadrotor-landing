[project]
name = "mpc-quadrotor-landing"
version = "0.1.0"
description = "MPC-based autonomous quadrotor landing system"
requires-python = ">=3.13"
dependencies = [
    "sim",
]

[tool.uv.sources]
sim = { workspace = true }

[tool.uv.workspace]
members = ["src", "sim"]
